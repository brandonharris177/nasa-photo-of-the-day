import React from "react";
import "./Date.scss"

const DateCreator = (props) => {
  // console.log(props.url);
  return (
    <>
      <h2>{props.date}</h2>
    </>
  );
};

export default DateCreator;